[comment: 请在此处提供一个参考示例 "docs/integrations/arxiv.md"]::
[comment: 使用此模板在 "docs/integrations/" 中创建新的 .md 文件]::

# Title_REPLACE_ME

[comment: 只允许有一个 Title/H1!]::

>
[comment: 描述：在阅读此描述后，读者应决定此集成是否足够好可以尝试/继续阅读，或者]::
[comment: 继续阅读下一个集成文档。]::
[comment: 描述应包含一个指向源的链接以供继续阅读。]::

## 安装和设置

[comment: 安装和设置：所有必要的附加包安装和设置，例如 Token 等]::

```bash
pip install package_name_REPLACE_ME
```

[comment: 或者这段文字：]::

没有特殊的设置。

[comment: 下面的 H2/## 部分应包含集成模块的名称，例如 "LLM"，"Text Embedding Models" 等]::
[comment: 请参阅 "index.html" 页面的 "Modules" ]::
[comment: 每个 H2 部分都应包含一个指向示例的链接和一个包含集成类导入的 Python 代码]::
[comment: 以下是几个示例部分。请删除所有不必要的部分。添加所有必需但此处未提供的部分。]::

## LLM

请参阅[用法示例](/docs/integrations/llms/INCLUDE_REAL_NAME)。

```python
from langchain_community.llms import integration_class_REPLACE_ME
```

## Text Embedding Models

请参阅[用法示例](/docs/integrations/text_embedding/INCLUDE_REAL_NAME)。

```python
from langchain_community.embeddings import integration_class_REPLACE_ME
```

## Chat models

请参阅[用法示例](/docs/integrations/chat/INCLUDE_REAL_NAME)。

```python
from langchain_community.chat_models import integration_class_REPLACE_ME
```

## Document Loader

请参阅[用法示例](/docs/integrations/document_loaders/INCLUDE_REAL_NAME)。

```python
from langchain_community.document_loaders import integration_class_REPLACE_ME
```