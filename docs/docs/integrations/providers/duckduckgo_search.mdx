# DuckDuckGo 搜索

>[DuckDuckGo Search](https://github.com/deedy5/duckduckgo_search) 是一个使用
> `DuckDuckGo.com` 搜索引擎来搜索单词、文档、图片、视频、新闻、地图和文本翻译的包。它会将文件和图片下载到本地硬盘。

## 安装和设置

你需要安装一个 python 包：

```bash
pip install duckduckgo-search
```

## 工具

请参阅 [使用示例](/docs/integrations/tools/ddg)。

有两个工具可用：

```python
from langchain_community.tools import DuckDuckGoSearchRun
from langchain_community.tools import DuckDuckGoSearchResults
```