# Groq

>[Groq](https://groq.com) 开发了世界上第一个语言处理单元™，即 `LPU`。
> `Groq LPU` 拥有确定性的单核流式架构，为任何给定工作负载提供可预测和可重复的性能，树立了生成式 AI 推理速度的标准。
>
>除了架构之外，`Groq` 软件旨在通过您所需的工具赋能开发者，帮助您创建创新、强大的 AI 应用。
>
>以 Groq 为引擎，您可以：
>* 为实时 AI 和 HPC 推理实现无损的低延迟和高性能 🔥
>* 了解任何给定工作负载的确切性能和计算时间 🔮
>* 利用我们的尖端技术保持竞争优势 💪

## 安装和设置

安装集成包：

```bash
pip install langchain-groq
```

申请一个 [API 密钥](https://console.groq.com/login?utm_source=langchain&utm_content=provider_page) 并将其设置为环境变量：

```bash
export GROQ_API_KEY=gsk_...
```

## 聊天模型

请参阅 [用法示例](/docs/integrations/chat/groq)。

```python
from langchain_groq import ChatGroq