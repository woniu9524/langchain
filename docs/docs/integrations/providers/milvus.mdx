# Milvus

>[Milvus](https://milvus.io/docs/overview.md) 是一个数据库，用于存储、索引和管理由深度神经网络及其他机器学习 (ML) 模型生成的海量嵌入向量。


## 安装和设置

安装 Python SDK：

```bash
pip install langchain-milvus
```

## 向量存储

请参阅[用法示例](/docs/integrations/vectorstores/milvus)。

导入此向量存储：
```python
from langchain_milvus import Milvus
```