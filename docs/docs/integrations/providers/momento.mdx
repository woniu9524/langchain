# Momento

> [Momento Cache](https://docs.momentohq.com/) 是世界上第一个真正的无服务器缓存服务，提供即时弹性、按需扩展（scale-to-zero）能力和极快的性能。
>
> [Momento Vector Index](https://docs.momentohq.com/vector-index) 是最高效、最易用、完全无服务器的向量索引。
>
> 对于这两项服务，只需获取 SDK，获取 API 密钥，在代码中输入几行即可开始使用。它们共同为您的 LLM 数据需求提供了一个全面的解决方案。

本页面介绍如何在 LangChain 中使用 [Momento](https://gomomento.com) 生态系统。

## 安装和设置

- 在此[此处](https://console.gomomento.com/)注册免费账户以获取 API 密钥
- 使用 `pip install momento` 安装 Momento Python SDK

## Cache

将 Momento 用作 LLM 提示和响应的无服务器、分布式、低延迟缓存。标准缓存是 Momento 用户在任何环境下的主要用例。

要将 Momento Cache 集成到您的应用程序中：

```python
from langchain.cache import MomentoCache
```

然后，使用以下代码进行设置：

```python
from datetime import timedelta
from momento import CacheClient, Configurations, CredentialProvider
from langchain.globals import set_llm_cache

# 实例化 Momento 客户端
cache_client = CacheClient(
    Configurations.Laptop.v1(),
    CredentialProvider.from_environment_variable("MOMENTO_API_KEY"),
    default_ttl=timedelta(days=1))

# 选择您选择的 Momento 缓存名称
cache_name = "langchain"

# 实例化 LLM 缓存
set_llm_cache(MomentoCache(cache_client, cache_name))
```

## Memory

Momento 可用作 LLM 的分布式内存存储。

请参阅[此笔记本](/docs/integrations/memory/momento_chat_message_history)了解如何将 Momento 用作聊天消息历史记录的内存存储的演练。

```python
from langchain.memory import MomentoChatMessageHistory
```

## Vector Store

Momento Vector Index (MVI) 可用作向量存储。

请参阅[此笔记本](/docs/integrations/vectorstores/momento_vector_index)了解如何将 MVI 用作向量存储的演练。

```python
from langchain_community.vectorstores import MomentoVectorIndex