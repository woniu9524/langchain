# PromptLayer

>[PromptLayer](https://docs.promptlayer.com/introduction) 是一个用于提示工程的平台。
> 它还提供 LLM 可观测性功能，用于可视化请求、版本化提示和跟踪使用情况。
>
>虽然 `PromptLayer` 确实有与 LangChain 直接集成的 LLM（例如
> [`PromptLayerOpenAI`](https://docs.promptlayer.com/languages/langchain)），
> 但使用回调是集成 `PromptLayer` 与 LangChain 的推荐方式。

## 安装和设置

要使用 `PromptLayer`，我们需要：
- 创建一个 `PromptLayer` 账户
- 创建一个 API 令牌并将其设置为环境变量 (`PROMPTLAYER_API_KEY`)

安装 Python 包：

```bash
pip install promptlayer
```


## 回调

请参阅[用法示例](/docs/integrations/callbacks/promptlayer)。

```python
import promptlayer  # 别忘了导入！
from langchain.callbacks import PromptLayerCallbackHandler
```


## LLM

请参阅[用法示例](/docs/integrations/llms/promptlayer_openai)。

```python
from langchain_community.llms import PromptLayerOpenAI
```


## Chat Models

请参阅[用法示例](/docs/integrations/chat/promptlayer_chatopenai)。

```python
from langchain_community.chat_models import PromptLayerChatOpenAI
```