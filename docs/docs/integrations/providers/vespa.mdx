# Vespa

>[Vespa](https://vespa.ai/) 是一个功能齐全的搜索引擎和向量数据库。
> 它支持向量搜索 (ANN)、词汇搜索和结构化数据搜索，所有这些都可以在同一个查询中完成。

## 安装和设置

```bash
pip install pyvespa
```

## Retriever

请参阅[用法示例](/docs/integrations/retrievers/vespa)。

```python
from langchain.retrievers import VespaRetriever
```