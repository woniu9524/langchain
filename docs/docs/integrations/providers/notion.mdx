# Notion DB

>[Notion](https://www.notion.so/) 是一个协作平台，支持修改后的 Markdown，集成了看板、任务、Wiki 和数据库。它是一个用于笔记记录、知识和数据管理以及项目和任务管理的一体化工作空间。

## 安装和设置

所有说明都在下面的示例中。

## 文档加载器

我们有两个不同的加载器：“`NotionDirectoryLoader`” 和 “`NotionDBLoader`”。

在此处查看使用示例：[/docs/integrations/document_loaders/notion](/docs/integrations/document_loaders/notion)。


```python
from langchain_community.document_loaders import NotionDirectoryLoader, NotionDBLoader
```