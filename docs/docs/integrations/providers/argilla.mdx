# Argilla

>[Argilla](https://argilla.io/) 是一个开源的 LLM 数据策展平台。
> 使用 `Argilla`，通过结合人类和机器的反馈，可以更快地进行数据策展，从而构建出强大的语言模型。`Argilla` 支持 MLOps 周期中的每个步骤，涵盖数据标注到模型监控。

## 安装和设置

获取您的 [API 密钥](https://platform.openai.com/account/api-keys)。

安装 Python 包：

```bash
pip install argilla
```

## 回调

```python
from langchain.callbacks import ArgillaCallbackHandler
```

请参阅 [示例](/docs/integrations/callbacks/argilla)。