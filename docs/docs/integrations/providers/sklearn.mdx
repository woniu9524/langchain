# scikit-learn

>[scikit-learn](https://scikit-learn.org/stable/) 是一个开源的机器学习算法集合，
> 其中包含一些 [k 近邻](https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.NearestNeighbors.html) 的实现。`SKLearnVectorStore` 封装了此实现，并增加了以 json、bson（二进制 json）或 Apache Parquet 格式持久化向量存储的可能性。

## 安装和设置

- 使用 `pip install scikit-learn` 安装 Python 包


## 向量存储

`SKLearnVectorStore` 在 scikit-learn 包的近邻实现之上提供了一个简单的封装，允许您将其用作向量存储。

导入此向量存储：

```python
from langchain_community.vectorstores import SKLearnVectorStore
```

有关 SKLearnVectorStore 封装的更详细的教程，请参阅 [此笔记本](/docs/integrations/vectorstores/sklearn)。


## Retriever

`支持向量机 (SVM)` 是用于分类、回归和异常检测的监督学习方法。

请参阅 [用法示例](/docs/integrations/retrievers/svm)。

```python
from langchain_community.retrievers import SVMRetriever