# Wolfram Alpha

>[WolframAlpha](https://en.wikipedia.org/wiki/WolframAlpha) 是由 `Wolfram Research` 开发的答案引擎。
> 它通过计算外部来源的数据来回答事实性查询。

本页介绍如何在 LangChain 中使用 `Wolfram Alpha API`。

## 安装和设置
- 使用以下命令安装要求
```bash
pip install wolframalpha
```
- 前往 [Wolfram Alpha](https://developer.wolframalpha.com/) 注册一个开发者账户
- 创建一个应用并获取您的 `APP ID`
- 将您的 APP ID 设置为环境变量 `WOLFRAM_ALPHA_APPID`


## Wrappers

### Utility

有一个 WolframAlphaAPIWrapper 工具可以包装此 API。导入此工具：

```python
from langchain_community.utilities.wolfram_alpha import WolframAlphaAPIWrapper
```

有关此工具的更详细演示，请参阅 [此笔记本](/docs/integrations/tools/wolfram_alpha)。

### Tool

您也可以轻松地将此工具加载为 Tool（与 Agent 一起使用）。
您可以通过以下方式执行此操作：
```python
from langchain_community.agent_toolkits.load_tools import load_tools
tools = load_tools(["wolfram-alpha"])
```

有关工具的更多信息，请参阅 [此页面](/docs/how_to/tools_builtin)。