# Context

>[Context](https://context.ai/) 为由 LLM 支持的产品和功能提供用户分析。

## 安装和设置

我们需要安装 `context-python` Python 包：

```bash
pip install context-python
```

## 回调

请参阅 [用法示例](/docs/integrations/callbacks/context)。

```python
from langchain.callbacks import ContextCallbackHandler
```