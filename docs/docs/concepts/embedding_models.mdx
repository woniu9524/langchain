# 嵌入模型
<span data-heading-keywords="embedding,embeddings"></span>

:::info[先决条件]

* [文档](https://python.langchain.com/api_reference/core/documents/langchain_core.documents.base.Document.html)

:::

:::info[注意]
此概念概述侧重于基于文本的嵌入模型。

嵌入模型也可以是多模态的(/docs/concepts/multimodality)，但此类模型目前不受 LangChain 支持。
:::

想象一下，能够将任何文本——一条推文、一份文档或一本书——捕捉成一种单一、紧凑的表示形式。
这就是嵌入模型的强大之处，它们是许多检索系统的核心。
嵌入模型将人类语言转化为机器能够理解和快速准确地进行比较的一种格式。
这些模型以文本作为输入，并生成一个固定长度的数字数组，这是文本语义含义的数字指纹。
嵌入使得搜索系统不仅可以基于关键词匹配，还可以基于语义理解来查找相关文档。

## 关键概念

![概念概述](/img/embeddings_concept.png)

(1) **将文本嵌入为向量**：嵌入将文本转化为数值向量表示。

(2) **测量相似度**：可以通过简单的数学运算来比较嵌入向量。

## 嵌入

### 历史背景

多年来，嵌入模型的格局发生了显著变化。
2018 年，Google 推出了 [BERT (基于 Transformer 的双向编码器表示)](https://www.nvidia.com/en-us/glossary/bert/)，这是一个重要的里程碑。
BERT 将 Transformer 模型应用于将文本嵌入为简单的向量表示，这在各种 NLP 任务中取得了前所未有的性能。
然而，BERT 并没有针对高效生成句子嵌入进行优化。
这一限制促生了 [SBERT (Sentence-BERT)](https://www.sbert.net/examples/training/sts/README.html) 的创建，它调整了 BERT 架构以生成语义丰富的句子嵌入，这些嵌入可以通过余弦相似度等相似性度量轻松进行比较，从而大大降低了查找相似句子等任务的计算开销。
如今，嵌入模型生态系统非常多样化，众多提供商都提供了自己的实现。
为了驾驭这种多样性，研究人员和从业者经常转向 Massive Text Embedding Benchmark (MTEB) [此处](https://huggingface.co/blog/mteb) 等基准进行客观比较。

:::info[进一步阅读]

* 请参阅 [开创性的 BERT 论文](https://arxiv.org/abs/1810.04805)。
* 请参阅 Cameron Wolfe 的 [精彩回顾](https://cameronrwolfe.substack.com/p/the-basics-of-ai-powered-vector-search?utm_source=profile&utm_medium=reader2) 关于嵌入模型。
* 请参阅 [Massive Text Embedding Benchmark (MTEB)](https://huggingface.co/blog/mteb) 排行榜，全面了解嵌入模型。

:::

### 接口

LangChain 提供了一个通用的接口来处理它们，为常见操作提供了标准方法。
这个通用接口通过两个核心方法简化了与各种嵌入提供商的交互：

- `embed_documents`：用于嵌入多个文本（文档）
- `embed_query`：用于嵌入单个文本（查询）

这种区别很重要，因为一些提供商对文档（要搜索的内容）和查询（搜索输入本身）采用不同的嵌入策略。
为了说明这一点，以下是一个使用 LangChain 的 `.embed_documents` 方法嵌入字符串列表的实际示例：

```python
from langchain_openai import OpenAIEmbeddings
embeddings_model = OpenAIEmbeddings()
embeddings = embeddings_model.embed_documents(
    [
        "Hi there!",
        "Oh, hello!",
        "What's your name?",
        "My friends call me World",
        "Hello World!"
    ]
)
len(embeddings), len(embeddings[0])
(5, 1536)
```

为了方便起见，您还可以使用 `embed_query` 方法来嵌入单个文本：

```python
query_embedding = embeddings_model.embed_query("What is the meaning of life?")
```

:::info[进一步阅读]

* 请参阅 [LangChain 嵌入模型集成](https://docs.langchain.com/docs/integrations/text_embedding/) 的完整列表。
* 请参阅这些关于使用嵌入模型的[操作指南](/docs/how_to/embed_text)。

:::

### 集成

LangChain 提供了许多嵌入模型集成，您可以在[嵌入模型](/docs/integrations/text_embedding/)集成页面上找到它们。

## 测量相似度

每个嵌入本质上是一组坐标，通常位于高维空间中。
在这个空间中，每个点（嵌入）的位置反映了其对应文本的含义。
就像相似的词语在同义词词典中可能彼此靠近一样，相似的概念在这个嵌入空间中也彼此靠近。
这使得对不同文本片段进行直观的比较成为可能。
通过将文本简化为这些数值表示，我们可以使用简单的数学运算来快速测量两段文本的相似程度，而无论它们原始的长度或结构如何。
一些常见的相似度度量包括：

- **余弦相似度**：测量两个向量之间夹角的余弦值。
- **欧氏距离**：测量两点之间的直线距离。
- **点积**：测量一个向量在另一个向量上的投影。

相似度度量的选择应基于模型。
例如，[OpenAI 建议对他们的嵌入使用余弦相似度](https://platform.openai.com/docs/guides/embeddings/which-distance-function-should-i-use)，这可以轻松实现：

```python
import numpy as np

def cosine_similarity(vec1, vec2):
    dot_product = np.dot(vec1, vec2)
    norm_vec1 = np.linalg.norm(vec1)
    norm_vec2 = np.linalg.norm(vec2)
    return dot_product / (norm_vec1 * norm_vec2)

similarity = cosine_similarity(query_result, document_result)
print("Cosine Similarity:", similarity)
```

:::info[进一步阅读]

* 请参阅 Simon Willison 关于嵌入和相似度度量的[精彩博文和视频](https://simonwillison.net/2023/Oct/23/embeddings/)。
* 请参阅 Google 关于与嵌入相关的相似度度量的[文档](https://developers.google.com/machine-learning/clustering/dnn-clustering/supervised-similarity)。
* 请参阅 Pinecone 关于相似度度量的[博文](https://www.pinecone.com/learn/vector-similarity/)。
* 请参阅 OpenAI 关于使用 OpenAI 嵌入的相似度度量[常见问题解答](https://platform.openai.com/docs/guides/embeddings/faq)。

:::